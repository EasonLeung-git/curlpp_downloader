### a downloader by curlpp.
``` c++
lib_dir: curl_curlpp_lib
source:  ver01 (single thread; no UI)
         ver02 (multithread; no UI)
         ver03 (single thread; add UI)
         ver04 (multithread; add UI)
         ver05 (multithread, using moveToThread; add UI)
```
